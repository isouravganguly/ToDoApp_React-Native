import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    padding: 10,
    borderRadius: 50,
  },

  text: {
    color: "#080808",
    fontWeight: "400",
  },

  listComponent:{
    marginTop: 20,
  }
});

export default styles;
